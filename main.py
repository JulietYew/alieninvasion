#A program that listens to speech and answer questions.
#PyAudio allows us to record and play audio on a variety of platforms.
import pyaudio
#websockets allows a two way interactive communication session between the user's browser and the server.
import websockets
#asyncio allows for concurrent code usimng the async syntax
import asyncio
#this module provides function for encoding binary data to ASCII characters and vice versa.
import base64
#work with json data
import json
